import { ShorthandPropertyAssignment } from "typescript";

export type TypeUser = {
    name:string;
    email:string;
  }

export type TypeProduct = {
  nombre: string;
  precio: number;
  imagen: string;
}