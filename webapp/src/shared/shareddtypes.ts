import { ShorthandPropertyAssignment } from "typescript";

export type User = {
    name:string;
    email:string;
  }

export type Product = {
  nombre: string;
  precio: number;
}

export type TypeProduct = {
  id: string;
  nombre: string;
  precio: number;
  imagen: string;
}